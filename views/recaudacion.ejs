<!DOCTYPE html>
<html lang="es">
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<meta name="google" content="notranslate" />
	<title>Sistema - Recaudación</title>
	<link rel="icon" href="resources/images/copymaster.ico">
	<link rel="preconnect" href="https://fonts.gstatic.com">
	<link href="https://fonts.googleapis.com/css2?family=Roboto+Condensed:wght@700&display=swap" rel="stylesheet">
	<link href="https://fonts.googleapis.com/css2?family=Roboto+Condensed:wght@300&display=swap" rel="stylesheet">
	<link rel="stylesheet" type="text/css" href="resources/css/style.css">
	<link href="//cdn.jsdelivr.net/npm/@sweetalert2/theme-dark@4/dark.css" rel="stylesheet">
</head>
<body>
<% if (login) { %>
	<div class="center-registro center-recaudacion">
		<h1 class="registro-title">RECAUDACIÓN</h1>
		<form action="/recaudacion_send" method="POST" class="registro-form registro-form__rec">
			<input type="date" id="fecha" name="fecha" min="2021-01-01" max="2021-12-31" class="input-fecha">
			<label class="recaudacion_pesos">$</label>
			<input type="number" name="recaudacion" id="recaudacion" class="registro-input registro-input__rec">
			<div class="btn-registro">
				<input type="submit" class="registro-btn" value="ENVIAR">
			</div>
		</form>
		<div class="btn-registro">
			<a class="registro-btn btn-volver" href="/home_send">VOLVER</a>
		</div>
	</div>

	<script>
		const date = new Date();
		let year = date.getFullYear();
		let month = date.getMonth() + 1;
		month = (month < 10 ? "0" : "") + month;
		let day  = date.getDate();
		day = (day < 10 ? "0" : "") + day;

		const inputFecha = document.getElementById('fecha');
		inputFecha.value = `${year}-${month}-${day}`;

	</script>

<% } else { %>
	<script src="//cdn.jsdelivr.net/npm/sweetalert2@10/dist/sweetalert2.min.js"></script>
	<script>
			Swal.fire({
				title: '<%= alertTitle %>',
				text: '<%= alertMessage %>',
				icon: '<%= alertIcon %>',
				background: '<%= background %>',
				showConfirmButton: <%= showConfirmButton %> ,
				confirmButtonColor: '<%= confirmButtonColor %>',
				timer: <%= timer %>
			}).then(()=>{
				window.location='<%= ruta %>'
			})
	</script>
<% } %>

	<script src="//cdn.jsdelivr.net/npm/sweetalert2@10/dist/sweetalert2.min.js"></script>

	<% if(typeof alert != "undefined") { %>
		<script>
			Swal.fire({
				title: '<%= alertTitle %>',
				text: '<%= alertMessage %>',
				icon: '<%= alertIcon %>',
				background: '<%= background %>',
				showConfirmButton: <%= showConfirmButton %> ,
				confirmButtonColor: '<%= confirmButtonColor %>',
				timer: <%= timer %>
			}).then(()=>{
				window.location='<%= ruta %>'
			})
		</script>
	<% } %>
</body>
</html>